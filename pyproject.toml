[build-system]
requires = ["uv_build>=0.11,<0.13"]
build-backend = "uv_build"

[tool.uv.build-backend]
# Distribution name is fastapi-better-auth-bridge; the import package stays fastapi_better_auth.
module-name = "fastapi_better_auth"

[project]
name = "fastapi-better-auth-bridge"
version = "0.1.0"
description = "Verify Better Auth (TypeScript) sessions from FastAPI. A bridge, not a Python port."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Mulugeta Solomon" }]
keywords = ["better-auth", "fastapi", "authentication", "session", "jwt", "jwks"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Framework :: FastAPI",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Internet :: WWW/HTTP :: Session",
    "Topic :: Security",
    "Typing :: Typed",
]
dependencies = [
    "fastapi>=0.133",
    "starlette>=1.3.1",
    "pydantic>=2.7",
    "pyjwt[crypto]>=2.10",
    "anyio>=4",
    "exceptiongroup>=1.2; python_version < '3.11'",
]

[project.optional-dependencies]
httpx = ["httpx>=0.27"]
httpx2 = ["httpx2>=2.0"]
redis = ["redis>=5.0.1"]
sqlalchemy = ["sqlalchemy[asyncio]>=2.0"]

[dependency-groups]
dev = [
    "aiosqlite>=0.20",
    "asyncpg>=0.30",
    "exceptiongroup>=1.2",
    "httpx>=0.27",
    "httpx2>=2.0",
    "pyright>=1.1.411",
    "pytest>=8",
    "pytest-cov>=7",
    "redis>=5.0.1",
    "ruff>=0.6",
    "sqlalchemy[asyncio]>=2.0",
    "trio>=0.27",
    "typing-extensions>=4.12",
]

[project.urls]
Homepage = "https://github.com/Mulugeta-Solomon/fastapi-better-auth"
Repository = "https://github.com/Mulugeta-Solomon/fastapi-better-auth"
Issues = "https://github.com/Mulugeta-Solomon/fastapi-better-auth/issues"

[tool.pytest.ini_options]
addopts = "-m 'not e2e'"
filterwarnings = ["error"]
markers = [
    "e2e: conformance tests against the live harness (harness/docker-compose.yml); run with -m e2e",
]

[tool.ruff]
line-length = 100

[tool.ruff.lint.flake8-bugbear]
# `Depends(...)` in a parameter default is how FastAPI declares a dependency; the object
# is inert and per-request state lives in the resolver, so B008's mutable-default hazard
# does not apply. Only the marker itself is exempted, never the callables passed to it.
extend-immutable-calls = ["fastapi.Depends", "fastapi.Security"]

[tool.pyright]
include = ["src", "tests"]
typeCheckingMode = "strict"
reportUnnecessaryTypeIgnoreComment = true
pythonVersion = "3.10"
venvPath = "."
venv = ".venv"
